## Packages
framer-motion | For smooth page transitions and UI animations
clsx | For conditional class names
tailwind-merge | For merging tailwind classes safely

## Notes
The translation logic relies on `@shared/translator`.
Frontend needs to fetch all words to build the reverse lookup map.
Dark mode is the default and only theme.
